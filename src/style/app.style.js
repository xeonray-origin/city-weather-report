import { createStyles } from "@material-ui/core/styles";

export default createStyles({
  root: {
    flexGrow: 1,
  },
  select: {
    width: "100%",
    fontSize: "1.8rem",
    marginLeft: "auto",
    marginRight: "auto",
    textJustify: "center",
  },
});
